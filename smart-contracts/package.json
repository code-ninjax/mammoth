{
  "name": "mammoth-smart-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rimraf cache artifacts",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:bdag": "hardhat run --network bdag scripts/deploy.ts",
    "demo:local": "hardhat run --network hardhat scripts/demo.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}